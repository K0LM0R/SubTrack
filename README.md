# SubTrack — трекер підписок

Вебзастосунок для керування щомісячними підписками (стримінг, музика, хмара тощо).  
Дані зберігаються у файлі `data.json` на сервері.

## Запуск

1. Встановіть Python 3.8+ та Flask:
   ```bash
   pip install -r requirements.txt